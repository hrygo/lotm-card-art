# 卡牌载体、框徽接口与已接受实例

版本：1.2.0。当前完整工艺见 [制作SOP v3](production-sop-v3.md)；本文件维护载体细则、EmblemDock 接口与实测边界，不重复生产主流程。架构决策见 [ADR-001](decisions/ADR-001-emblem-dock-ownership.md) 与 [ADR-002](decisions/ADR-002-agentic-mother-deterministic-inheritance.md)。

## 载体关系

卡牌背板是正面承载面，不等于卡背，也不意味着必须另外生成一张可见底图。当前采用人物与完整环境一体生成，框、融合徽、姓名独立叠回。共享的是几何与接口，不是22途径统一石板或同一底色。

## 当前定稿：EmblemDock

后续新母版采用“母版固定嵌座，圣徽本体独立装配”的结构。母版框架必须提供固定的视觉嵌孔、暗腔、承托面、接触受光、前缘遮挡和保护 mask；圣徽只提供途径母标识、序列数字及自身材质。确定性合成器负责按固定锚点装配、生成一次接触阴影并记录门禁结果。

这里的孔洞默认是视觉嵌孔，不是让主体插画透出的真实透明孔。圣徽不得携带完整边框、完整承托、任意底板或第二套阴影。母版、圣徽和合成器的像素职责以 [`emblem-dock-interface-v1.json`](../production/templates/emblem-dock-interface-v1.json) 及 [`emblem-dock.schema.json`](../production/schemas/emblem-dock.schema.json) 为准。

这是一条新的通用载体架构；下文的历史批准实例保留其当时的无中央托座方案，不因本节自动改图或获得新结构批准。

生成完整场景时实际附上身份框。输出不含框、徽、姓名和说明文字。框下场景连续，外轮廓由独立遮罩限制；在顶部U形开口处检查矩形溢出。复制框、第二道窗口边和错向投影是返修对象，不靠宽羽化或全图调色掩盖。

## 衔接件合同

输入包含母框与圣徽真实来源/摘要、两端接触点、可见区、允许遮挡区、孔洞保护区、材质厚度、光向、采样与层序。生成稿带出的旧框或额外框肩不作为新几何依据。

填写 [EmblemDock 接口模板](../production/templates/emblem-dock-interface-v1.json)，并以 `task.contracts` 绑定实际引用的 mask/资产；机器字段约束见 [`emblem-dock.schema.json`](../production/schemas/emblem-dock.schema.json)。模板声明坐标系、固定变换、嵌座结构、接收域和保护域；它仍不是现有 Swift 自动解析的完整配方。零漂移验收含内部构件，而非仅外框轮廓。

工艺选择遵循途径：每条途径设计自己的 EmblemDock 形状语言。愚者的双侧帷幕可以作为嵌座承托的一部分，在外缘实心结构后收束；其他途径不得照搬其具体形态。末端不穿过数字镂空。上下文充分的局部生成与完整试装一起验收，不保证任意参考编辑零漂移。

净底时区分背景与银色材质；边缘连通只能去掉与外部相连的区域，封闭孔洞另查。保留原图；深/浅底与实际装配分别观察。已知裁口或残片依赖原框遮挡的层，仅限该配方使用；成为公共素材前需独立净化、明确接口遮罩并重新验证。

## 品质

五档主色唯一来源为 [配色配置](../config/quality-color-tokens.json)，重点用于纹理与宝石，不统一染色主体或整枚徽章。五档保持结构与数量，差异来自受控材质/宝石设计。圣者与天使已经拆为不同视觉档；旧四档入口仅代表历史实现。

框纹理、承接帷幕及相应宝石归同一 material_group，联合设计受光和反射。纹理、晶体与保护域分别设 mask，保留体色暗部、折射层次、明亮切面和局部火彩；不因框已换色而留下异档连接件，也不以统一 RGB 平涂冒充自然融合。

## 已接受实例：愚者序列9克莱恩

- 图像：`artifacts/production/klein-drape-study-v1/final.png`。
- 批准：[视觉批准记录](../production/approvals/klein-drape-v1-visual.json)。范围为该最终图的视觉效果，不含后续五档新配色。
- 原过程：[一体场景复盘](research/2026-09-13-klein-integrated-crest-study.md)、[帷幕试装记录](research/2026-09-13-klein-drape-study.md)。其中“未批准”是当时状态，现状以新增批准记录为准，不篡改历史。
- 方法：去除“占卜家”标题，双侧帷幕收束，无中央托座；场景、姓名、原框独立。
- 画布2048×3072，原场景1024×1536，包含重采样。
- 圣徽目标可见高353.28px，即11.5%；其目标区域中心(1024,251.64)。这是此卡参数。
- 姓名字形实际中心(1024,2670)，误差(0,0)px。字形来自Agentic，程序转为单色烟紫墨，不宣称是生成图原有金属色。
- 参数与输入清单见原输出manifest及`tools/render/klein_carrier.swift`。裁切分数、净底阈值与行轮廓保护是此素材的专用实现，不是通用分割算法。

## 当前可运行分支

`tools/render/klein_carrier.swift` 的 `render-drape ROOT OUT SCENE NAME BARE_FRAME EMBLEM JUNCTION` 生成该单卡分层；`gate ROOT OUT` 验证输入/输出摘要与确定性RGBA重建。OUT为仓库内未存在的`artifacts/production/klein-*`目录。

这个分支没有自动读取五档色表、没有自动记录全部新侧车依赖，也没有提供正式release。原card.json仍有旧文字身份cue；取消标题后的正式语义契约尚未同步，视觉批准不能掩盖这一差异。通用生产工具与专用装配器的接入缺口仍需实施验证。

`tools/render/foolkit.swift`保留旧四档与15%试装行为；详见对应Skill的历史运行说明。不要把它用于交付新五档而不先修改实现。

## 回退与扩展

认可的0/4/7/9原始融合成果及被引用版本保留。新结构建立新geometry_id，不覆盖旧框或重命名漂移稿充当母版。新配色/新结构扩展到五档十徽逐项复核，不能由单张卡面的认可推导全族批准。默认不增加其他途径、清理旧素材或发布。
