# 双侧帷幕收束试装

后续状态：用户已明确“通过”，视觉批准独立记录于`production/approvals/klein-drape-v1-visual.json`，绑定当前图像和manifest摘要；下文保留制作时状态。随后SOP与五个Skills已优化，新五档配色及有层次的色相流动、明亮宝石纳入合同，未重画或重着色本图。新版入口为`docs/production-sop-v2.md`。

此次Skill更新后，原承接件receipt因lotm-hierarchy摘要变化、原场景receipt因lotm-subject摘要变化，均被旧快照检查拒绝；未改写历史摘要。图像与manifest摘要核对通过，单卡专用RGBA重建gate仍通过。素材要参与新的生产任务时，须按新工艺重新编译、复核和登记，不能把旧receipt称为当前依赖全部有效。Skill结构校验五项均通过；这不等于新五档渲染器已实现。

2026-09-13。用户确认造型方向，尚未批准实际成图；本记录不是SOP。

## 产物

- 卡面：`artifacts/production/klein-drape-study-v1/final.png`，2048×3072 PNG/sRGB，包含重采样。
- 清单及独立身份、姓名、框、徽、场景、连接件、白底诊断与空标题层：同目录。
- 结构化任务：`production/tasks/fool-drape-junction.json`。
- 实际内置image_gen提示词、附件、时间及观察：`production/calls/fool-drape-junction-v1.json`。
- 原图与回执：`artifacts/production/fool-drape-junction/v001/`。

## 实际变化

“占卜家”标题层改为全透明；姓名仍为克莱恩·莫雷蒂。取消原浅拱与中央椭圆托座，使用新生成的双侧帷幕延伸段。生成稿包含额外框肩，程序仅提取延伸段；原框肩未被替换。连接层位于原框与原圣徽之后，圣徽行轮廓之间的透明/半透明区禁止连接件贡献，避免穿过数字镂空。

净底沿用针对此类中性背景的边缘连通法。延伸段外侧裁口及少量裁切残片依赖原框遮挡，因此当前连接图层是本卡定制装配层，不宣称是可任意摆放的通用净底素材。白底诊断与实际卡面已观察。

圣徽沿用11.5%目标占高及既定锚点。框、姓名、场景图层与上版逐文件SHA256一致。实际姓名中心(1024,2670)，双轴误差0px。人物与背景没有重新生成。

## 验收边界

专用gate重建全部输出RGBA与摘要并通过，scaffold与git diff --check通过。没有把新结构自动升级为四档母框，也没有批准正式发布。

本次按用户指示取消序列名文字，当前卡面仅通过融合9徽章承担途径/序列识别。原card.json的文字身份cue尚未改写，正式语义契约与release尚未重新批准；试装图不冒充原设计文字契约完全一致的发布成品。

原图、旧承托和旧卡面均保留；SOP保持未固化。

全库回归实测：116项，73.923秒，8个error，均为已有历史样片依赖`pathways/fool/sequences/09/card.json`过期；专用原生测试通过。未放宽门禁、未改写历史摘要，不宣称全库通过。
