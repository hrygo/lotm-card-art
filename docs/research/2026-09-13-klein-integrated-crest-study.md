# 克莱恩序列9：一体场景与顶部承托试装

状态：已生成、已合成、专用工程门禁通过；待用户视觉确认，不是正式 release。未修改 SOP 或覆盖原框套件。

## 当前产物

- 最终试装：`artifacts/production/klein-seer-crest-study-v2/final.png`
- 独立身份框：同目录 `identity.png`；独立框、徽、姓名、连接件、场景和遮罩均保留。
- 清单：同目录 `manifest.json`，绑定输入、工具源码、输出摘要和实际姓名中心。
- 一体场景：`artifacts/production/klein-seer-integrated/v002/raw.png`。
- 顶部承托原图：`artifacts/production/fool-crest-bridge-study/v001/raw.png`。
- 实际提示词、附件摘要、调用时间与观察：`production/calls/klein-seer-integrated-v1.json`、`klein-seer-integrated-v2.json`、`fool-crest-bridge-study-v1.json`。

## 已执行路线

完整框徽、姓名与“占卜家”标题先构成真实参考图，作为内置 image_gen 的附件。模型只输出人物与背景一体场景，程序叠回身份层。第一版人物过大、材料被姓名牌遮挡；第二次只调整场景构图，保留人物身份与灵摆事件。

用户随后要求改善框徽结合。当前候选从原始透明低档框和净底9徽章重新装配，不从已融合整框擦除圣徽。侧轨、框肩、姓名牌不单独变形。徽章按可见字形等比装入目标高353.28px的区域，占3072px卡高11.5%；目标区域中心为(1024,251.64)，标题中心Y=500。原15%与旧锚点保留在历史套件，不把这次主动调整称为相对旧版零位移。

新增Agentic浅拱承托件连接两侧框肩，在圣徽后方形成安装支撑。原图实际为2172×724、无alpha、带烘焙棋盘；程序按24色差阈值做边缘连通中性背景净化，保留被金色轮廓包围的银色材质。该方法仅用于此素材，不是通用抠图。净底后的深底、白底与实际卡面均已观察，当前卡面尺度未见明显棋盘块或独立矩形边界；不承诺微观边缘完美。

## 当前观察

- 圣徽由画面内悬浮标志变为顶部承托徽饰；原Agentic数字与面具造型保留。
- 主体、背景连续，未使用单独可见背板或矩形插画窗口。
- 人物头发不再压住标题；灵摆、茶杯、黑瓶、星点晶体与深色空杯在姓名牌上方可见。
- 主图无说明文字。书页有抽象灰色图纹，不承担精确配方传达。材料意象不能等同于完整配方、用量或晋升条件的可回读证明。
- 姓名为“克莱恩·莫雷蒂”，复用Agentic字形，程序转换为单色烟紫墨；实际字形中心(1024,2670)，双轴误差(0,0)px。
- 承托件是独立结构试稿，不是已获批准的四档新母框；标题下方留白、肩部与连接件的视觉重量仍由用户审阅。

## 实测与限制

2026-09-13：专用gate按输入摘要重建并比较全部输出RGBA，通过；实心身份像素有覆盖保护检查，半透明像素按确定性source-over重建，不能说最终半透明边缘与源RGBA完全相同。新增净底背景/封闭银面保留/身份覆盖反例先失败后通过。scaffold、单卡design、git diff --check通过。

全库116项测试已运行，结果为8个error，涉及历史样片对`pathways/fool/sequences/09/card.json`的过期依赖。未修改历史回执摘要或放宽门禁来消除错误。因此不能宣称完整旧生产框架全部通过，也未执行正式release批准。

源场景1024×1536；最终PNG/sRGB为2048×3072，包含重采样，不是原生2048×3072插画或超分细节重建。此轮所有旧素材均保留。效果认可后才将经过验证的步骤提炼进SOP与Skill。
