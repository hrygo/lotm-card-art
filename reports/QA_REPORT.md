# 交付前检查报告 — v0.3.0

## 本次结论
**脚手架可以使用；220张卡牌本身尚未完成，不宣称已有正式成品。**
已执行结构检查、独立JSON Schema校验、正常与反例单元测试、CLI试运行及目标逐项对照。

## 与用户目标对照
| 目标 | 实际落地 | 当前结论 |
|---|---|---|
| 22条途径、每序列一张 | 22个途径目录，每目录10个card.json | 220卡位，数量与ID完整 |
| 身份—扮演—能力—魔药—晋升—限制 | 每卡六维字段，合计1320语义位 | 结构完整，不冒充事实已填 |
| 表达形式灵活 | 10种载体、共享事件、三档精度、四种构图 | 不是六格文字栏，也不要求固定正文比例 |
| 风格与事实不能混淆 | direction为art_proposal，canon独立，名称种子显式未核验 | 22份原创路径提案，0份假正典 |
| 同套统一、途径差异、序列递进 | 全局骨架、22种形状/材质方案、相邻卡不可替代性 | 文本规则已检查，实图效果未测试 |
| 防止泛泛的气氛充数 | 每维intent/cue/readback/misreading_guard；单一笼统cue不能包办六项 | 软件检查计划结构，实图仍需回读 |
| AGENTS分层 | 根、领域、22个途径；单卡不复制AGENTS | 最长祖先链9703字节 |
| 独立高清 | 2:3；标准2048×3072，收藏4096×6144；原图与处理链分开 | 规格已定义，未生成图片 |
| 反复检查与可复验 | 测试、报告、清单、干净解压复测流程 | 软件检查通过，不替代看图 |

## 已执行的软件检查
- 48项单元测试全部通过；包含正例和拒绝坏数据的反例。
- 245个数据实例通过独立JSON Schema 2020-12验证：220张卡、22份canon、card/review/claim模板。
- 220张卡scaffold检查通过；220张的design/release门槛均如预期被阻断。
- 8项CLI正常/预期失败试运行通过；报告参数不能覆盖源AGENTS.md。
- Python3.10语法目标检查通过；实际运行环境Python 3.13.5。
- 全部文本UTF-8可读，无替换字符/NUL；没有字体文件、原著全文或卡牌图片混入。

## 重点反例
缺少魔药维度；六维共用一个笼统cue；以纯光线表达精确身份；没有可访问图例；
用未研究冒充未披露；把另一序列能力引用进来；不可访问来源假装已读；
译本直接当中文名称证据；解释冒充精确断言；空审核自动批准；旧设计摘要复用；
真实像素错误；CRC损坏；未记录放大；超分后冒称原生；越界路径与generated符号链接。

## 本次仍未执行
没有生成220张卡，没有完成220张的原著事实核验，没有图像审美/文字/六维实图回读，没有真实用户识别实验，没有用户批准基线。
31个序列名称为未核验种子，189个序列名称留空；22个途径标签亦为工作检索标签，不宣称正典校对完成。
不因脚手架测试通过就给卡牌打“质量通过”。软件合成的测试PNG与审核仅存在于临时测试目录，不打包为卡牌。

## 日志与复验
详见 `scaffold-check.json`、`audit-summary.json`、`expected-blocks.json`、`cli-smoke-tests.json` 与 `unit-tests.txt`。
解压后的重测结果另记录在 `clean-extraction-check.json`。SHA256SUMS负责交付文件完整性，不是设定真实性证明。

```bash
python3 tools/cardctl.py check --level scaffold
python3 -m unittest discover -s tests -v
python3 tools/cardctl.py verify-manifest
```
